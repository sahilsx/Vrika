@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="body">
    <div class="REQ">
        <h2>ORDER LIST</h2>
        <div class="table-responsive">
            <table class="table table-hover table-bordered">
                <thead>
                    <tr>
                        <th>PRODUCT-ID</th>
                        <th>NAME</th>
                        <th>ADDRESS</th>
                        <th>EMAIL</th>
                        <th>PHONE</th>
                        <th>CITY</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var Shipping in Model)
                    {
                        <tr>
                            <td>@Shipping.Productid</td>
                            <td>@Shipping.fullname</td>
                            <td>@Shipping.address</td>
                            <td>@Shipping.email</td>
                            <td>@Shipping.Phone</td>
                            <td>@Shipping.city</td>
                            <td>
                                <a class="btn btn-primary btn-sm" asp-controller="Ship" asp-action="Notify" asp-route-id="@Shipping.Id">NOTIFY</a>
                                <a class="btn btn-danger btn-sm" asp-controller="Ship" asp-action="Sdelete" asp-route-id="@Shipping.Id">Delete Request</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<style>
    .body {
        margin: 0 auto;
        padding: 10px;
        width: 100%;
        min-height: 90vh;
    }

    .REQ {
        width: 95%;
        max-width: 1200px;
        margin: 20px auto;
        background: #fff;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        padding: 20px;
        text-align: center;
    }

    h2 {
        margin-bottom: 20px;
        text-align: center;
        color: #333;
    }

    .table-responsive {
        overflow-x: auto;
        width: 100%;
    }

    .table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px auto;
    }

    .table th, .table td {
        padding: 12px;
        text-align: left;
        vertical-align: middle;
        border: 1px solid #ddd;
        font-size: 14px;
    }

    .table-hover tbody tr:hover {
        background-color: #f2f2f2;
    }

    .btn-primary {
        background-color: #007bff;
        color: #fff;
        border: none;
        padding: 6px 12px;
        border-radius: 3px;
        font-size: 12px;
        transition: background-color 0.3s ease;
    }

    .btn-primary:hover {
        background-color: #0056b3;
    }

    .btn-danger {
        background-color: #dc3545;
        color: #fff;
        border: none;
        padding: 6px 12px;
        border-radius: 3px;
        font-size: 12px;
        transition: background-color 0.3s ease;
    }

    .btn-danger:hover {
        background-color: #c82333;
    }

    /* Responsive Design */
   
</style>

